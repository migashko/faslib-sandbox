#ifndef FAS_SERIALIZATION_TAGS_HPP
#define FAS_SERIALIZATION_TAGS_HPP

namespace fas{ namespace serialization{

struct _container_;
struct _element_;
struct _entity_; // это основной - переименовать
struct _entity_piece_;
struct _sequence_each_;
struct _sequence_smart_;

//
  
struct _except_;
struct _clear_;
struct _status_;

struct _parse_;
struct _deser_;
struct _ser_;

}}

#endif
